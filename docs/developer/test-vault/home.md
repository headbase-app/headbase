# Home

This is a test vault with example content:
- `types` includes different file types
- `encoding` includes different text encodings
- `nested` includes nested files/folders
- `views` includes files and views
